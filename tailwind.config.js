/** @type {import('tailwindcss').Config} */
module.exports = {
  prefix: 'tw-', // Adds 'tw-' before all Tailwind utility classes  
  content: [
    './index.html', // Adjust this to your content paths
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
